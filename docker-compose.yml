version: '3.8'

services:
  grok-sdr-system:
    build: .
    ports:
      - "8081:8080"  # Changed from 8080:8080
    environment:
      - XAI_API_KEY=${XAI_API_KEY}
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
